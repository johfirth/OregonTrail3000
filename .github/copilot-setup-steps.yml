steps:
  - name: "Setup Node.js"
    uses: "actions/setup-node@v4"
    with:
      node-version: "20"

  - name: "Install dependencies"
    run: |
      echo "No dependencies to install yet — project is in design phase"
      echo "Future: npm install or pip install -r requirements.txt"
